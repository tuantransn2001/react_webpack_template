import React from "react";
import ReactDOM from "react-dom";

function App() {
  return <>Hello This is demo initial React project with webpack</>;
}

ReactDOM.render(<App />, document.getElementById("root"));
